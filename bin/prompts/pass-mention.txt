Read the skill at .opencode/skills/x-growth-automation/SKILL.md in this repo, then execute the Mention Bot Pass.

The X API has no credits. Do NOT use pitch-cli x-api commands. Drive ALL X actions strictly through the Playwright browser MCP tools (browser_navigate, browser_snapshot, browser_click, browser_type, browser_file_upload).

Account Safety:
- Ensure the logged-in profile in the browser is @trypitchdotco (the official brand handle).

HUMAN VOICE REQUIREMENT (CRITICAL):
- Never sound like ChatGPT/LLM. Zero em-dashes (—), no corporate fluff, no "game-changer", no "hope this finds you well".
- Write like a real tech founder talking peer-to-peer: concise, direct, helpful, natural rhythm.

STRICT IDEMPOTENCY (ZERO DOUBLE VIDEO RENDERING):
- Primary Key: tweet_id.
- BEFORE creating any video job on Pitch MCP or sending a receipt reply, check SQLite via ./target/release/pitch-cli db jobs.
- If a tweet_id is ALREADY in SQLite, skip it immediately to prevent duplicate video rendering.

FOLLOWER GATE RULE (VIRAL GROWTH MECHANIC):
- First Video Request for any user handle (@username): 100% FREE! No follow required.
- Repeat Video Request (user handle already has 1+ previous jobs in SQLite):
  * Check if @username is following @trypitchdotco in the browser (browser_navigate to https://x.com/@username and inspect profile buttons).
  * If NOT following @trypitchdotco:
    Do NOT trigger Pitch MCP video creation. Post follow gate reply in browser:
    "Hey @username! Glad you enjoyed your first demo! 🎬 To generate additional free product demos & launch videos, please follow @trypitchdotco first, then mention us again with your URL! 🚀"
    And save job in SQLite with status='follow_required'.

CANCELLATION & DELETION HANDLING:
- If a mention job has been deleted from SQLite or marked cancelled, stop polling its status immediately and do NOT post any video reply for it.

SMART INTENT DETECTION (LAUNCH VS DEMO VIDEO):
- Analyze the mention tweet text for intent:
  - If the user explicitly mentions "launch video", "launch demo", or "Product Hunt launch":
    * Video Type Label: "launch video"
    * Custom Instructions: "Create a high-energy, cinematic launch video of this product for Product Hunt / X launch. Highlight the core value proposition, key breakthrough features, and launch excitement."
    * Receipt Reply: "Cool @username, we're on it! 🚀 Generating your high-energy launch video for <url> now, we'll get back to you with the video link right here soon!"
    * Final Video Reply: "Here is your launch video @username generated by @trypitchdotco! Hope you enjoy it: <s3_video_url> 🚀"
  - Otherwise (Standard Product Demo):
    * Video Type Label: "demo video"
    * Custom Instructions: "Create a cinematic, polished product demo of this product. Highlight key features, value proposition, and user experience. Style guidelines: Use ocean background, Charon voice narration, light browser header, light theme, rounded container shape."
    * Receipt Reply: "Cool @username, we're on it! 🚀 Generating your cinematic demo for <url> now, we'll get back to you with the video link right here soon!"
    * Final Video Reply: "Here is your product demo @username generated by @trypitchdotco! Hope you enjoy it: <s3_video_url> 🎬"

Workflow:
1. Run ./target/release/pitch-cli circuit-breaker and ./target/release/pitch-cli budget. Stop if the breaker is tripped or budget is exhausted.
2. SCAN X FOR NEW MENTIONS & IDEMPOTENCY CHECK:
   - browser_navigate to https://x.com/search?q=%40trypitchdotco&f=live in Playwright Chromium.
   - Extract all recent tweets mentioning @trypitchdotco.
   - Compare with existing jobs in SQLite (./target/release/pitch-cli db jobs).
   - Apply FOLLOWER GATE RULE for repeat users.
   - If a new mention is found that is NOT in SQLite:
     a) Extract requesting handle (@username), tweet ID, tweet text, and product URL.
     b) Post smart instant receipt reply in browser ("launch video" vs "demo video").
     c) Trigger Pitch MCP video creation via ./target/release/pitch-cli mcp create <url> with custom instructions.
     d) Save job in SQLite with the returned jobId and status='rendering'.
3. POLL & DELIVER COMPLETED VIDEO DEMOS (NATIVE MP4 VIDEO UPLOAD + CLEANUP):
   - For all active rendering jobs in SQLite, check status via ./target/release/pitch-cli mcp status <jobId>.
   - If job is missing or deleted from SQLite, skip it.
   - When status is 'COMPLETED':
     a) Extract final S3 video URL from videoUrl field (e.g. https://s3.trypitch.co/.../final_with_cards.mp4).
     b) Download the MP4 video file locally to /tmp/demo.mp4 (e.g. via curl -s -o /tmp/demo.mp4 <s3_video_url>).
     c) Navigate to requesting user's tweet on X in browser (https://x.com/i/status/<tweet_id>).
     d) Click Reply. Attach /tmp/demo.mp4 directly to the tweet attachment input (input[data-testid="fileInput"]). Wait 15 seconds for X web UI to finish transcoding the MP4 video preview.
     e) Type smart text reply tagging user ("launch video" vs "product demo").
     f) Click Post/Reply to publish the native video tweet.
     g) Clean up temporary file: run rm -f /tmp/demo.mp4 in bash.
     h) Update SQLite job status to 'delivered' and s3_video_url to final S3 link.
4. End with a summary of new mentions ingested, receipts sent, and native video replies delivered.
