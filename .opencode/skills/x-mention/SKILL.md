---
name: x-mention
version: 1.0.0
description: >-
  Operate the @trypitchdotco viral mention-demo bot on X: instant receipt
  replies, Pitch MCP video generation, job polling, and final video delivery.
  Use when handling @trypitchdotco mentions on X, posting instant
  acknowledgements, generating demo videos via the Pitch MCP API
  (https://api.trypitch.co/mcp), delivering rendered S3 video links on X, or
  running the mention-demo pipeline via pitch-cli (inbox / worker / trigger /
  mcp / x-api).
license: MIT
compatibility: claude-code opencode
allowed-tools:
  - read
  - write
  - edit
  - bash
  - webfetch
---

# @trypitchdotco Viral Mention-Demo Bot Playbook

This playbook operates @trypitchdotco's automated viral mention demo bot using
**Instant Acknowledgement** + **Pitch MCP API (`https://api.trypitch.co/mcp`)**
+ **Official X API v2**.

## Instant Receipt & Delivery Sequence

1. **Step 1: Instant Receipt Reply (<1 Second)**
   - As soon as a user tweets `@trypitchdotco` with a product URL/domain,
     immediately post an instant acknowledgment reply on X via official API:
     > *"Cool @username, we're on it! 🚀 Generating your cinematic demo for [URL] now, we'll get back to you with the video link right here soon!"*

2. **Step 2: Submit Video Job via Pitch MCP API**
   - Triggers Pitch MCP tool `create_demo_video` for the target URL.
   - Captures returned `jobId`.

3. **Step 3: Track Job & Deliver Final Rendered Video S3 Link**
   - Polls MCP job status via `./target/release/pitch-cli mcp status [JOB_ID]`
     until status is `COMPLETED`.
   - Posts second reply on X with native MP4 attachment / direct S3 video link:
     > *"Here is your product demo @username generated by @trypitchdotco! Hope you enjoy it: https://s3.trypitch.co/trypitch/pitch/user_.../videos/.../final_with_cards.mp4 🎬"*

## Guardrails & operational rules

- **This flow is the real-time/delivery leg of the system**, run by the Rust
  webhook server (`./target/release/pitch-cli server`) and one-shot passes.
  It posts on @trypitchdotco (the product account) via official X API v2, not
  via the webbridge browser in most cases.
- Before running any mention pass, run `./target/release/pitch-cli circuit-breaker`
  (stop if exit 1) and `./target/release/pitch-cli budget`.
- **Idempotency:** never double-reply to the same mention. The SQLite
  `mention_jobs` table tracks tweet ID → jobId → status (`pending → rendering →
  delivered | failed | no_url_found`) and prevents duplicates.
- If the mention has no valid URL, mark the job `no_url_found` and skip delivery.
- Queue for human review if a video render fails repeatedly (`failed`) instead of
  spamming retries.
- Read `.opencode/skills/x-growth/safety.md` and
  `.opencode/skills/x-growth/voice.md` before any public text; run receipts and
  delivery replies through the `humanizer` skill where practical (the <1s receipt
  is time-critical, so keep the template warm and on-brand).
- The receipt and delivery replies are allowed to be template-cliched by design
  (this is a bot product play for reach), but still human-warm, truthful, and
  within `.opencode/skills/x-growth/voice.md` approved claims about what
  @trypitchdotco does.

## CLI pipeline

- `./target/release/pitch-cli inbox` — ingest mentions and trigger Pitch MCP jobs.
- `./target/release/pitch-cli worker` — poll Pitch MCP and deliver completed demos.
- `./target/release/pitch-cli trigger` — combined inbox + worker pass.
- `./target/release/pitch-cli mcp create <url>` / `mcp status <jobId>` / `mcp credits`.
- `./target/release/pitch-cli x-api mentions` / `reply <id> --text` / `post --text`.
- `./target/release/pitch-cli db jobs` — inspect the `mention_jobs` table.
- `./target/release/pitch-cli server` — the always-on webhook listener on
  http://0.0.0.0:8790 that can drive this flow automatically.